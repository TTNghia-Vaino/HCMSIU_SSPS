@model HCMSIU_SSPS.Models.SystemSetting

@{
    ViewData["Title"] = "Edit";
}

<h1 class="text-center mt-4">Edit System Setting</h1>

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            <h5 class="mb-0">Edit System Setting</h5>
        </div>
        <div class="card-body">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="SettingId" />

                <div class="form-group mb-3">
                    <label asp-for="SettingKey" class="form-label">Setting Key</label>
                    <input asp-for="SettingKey" class="form-control" />
                    <span asp-validation-for="SettingKey" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="SettingValue" class="form-label">Setting Value</label>
                    <input asp-for="SettingValue" class="form-control" />
                    <span asp-validation-for="SettingValue" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary w-100" />
                </div>
            </form>
        </div>
        <div class="card-footer text-center">
            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
